name: trim_map
channels:
  - conda-forge
  - bioconda
dependencies:
  - python<=3.10.6 # 3.10.13 does not work current installed conda version

  # preprocessing
  - trimmomatic

  # mapping
  - bwa==0.7.17
  - samtools
  - bwa-mem2==2.2.1
