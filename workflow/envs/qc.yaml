name: qc
channels:
  - conda-forge
  - bioconda # different order according to multiqc documentation
  - nodefaults
dependencies:
  # - python>=3.7 # multiqc error if lower
  - samtools
  - fastqc
  - qualimap
  - multiqc
  - bcftools
#  - bcftools==1.11 # version set by Nina, maybe can be removed. Causes error as of 6.3.24
