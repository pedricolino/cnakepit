name: qc
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # - python=3.7
  - samtools
  - fastqc
  - qualimap
  - multiqc
  - bcftools==1.11 # version set by Nina, maybe can be removed
